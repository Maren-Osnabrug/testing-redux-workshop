/*
This is an example of a redux duck test.
Import the duck to test and some Stub data for it.
 */

/* import moxios from 'moxios';
import configureMockStore from 'redux-mock-store';
import thunk from 'redux-thunk';

import * as duck from '../';

import Stub from '../stubs/.json';
import { api } from '../../http';

const middlewares = [thunk];
const mockStore = configureMockStore(middlewares);

beforeEach(() => {
	expect.assertions(1);
	moxios.install(api);
});

afterEach(() => moxios.uninstall(api)); */

// Action creators

// Selectors

// Functions

// Reducer
